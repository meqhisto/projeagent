## 2025-05-23 - Notification Bell Accessibility
**Learning:** Interactive icon buttons like Notification Bell often lack `aria-label` and focus states, making them invisible to screen readers and keyboard users.
**Action:** Always add `aria-label` with dynamic content (e.g., count) and `focus-visible` styles to such components.
