# Environment Variables Template

# ⚠️ IMPORTANT: Copy this file to .env and fill in the actual values
# DO NOT commit .env to git - it's in .gitignore

# ============================================
# Authentication Secrets
# ============================================
# Generate with: openssl rand -hex 32
AUTH_SECRET=your-secret-key-here-generate-with-openssl
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl

# ============================================
# Database Configuration
# ============================================
# Development (SQLite)
DATABASE_URL=file:./dev.db

# Production (PostgreSQL - recommended)
# DATABASE_URL=postgresql://user:password@postgres:5432/parselmonitor

# ============================================
# Application URLs
# ============================================
# Update these for production deployment
AUTH_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# ============================================
# Backend API URLs
# ============================================
# Public URL (used by browser)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Internal URL (used by Next.js server)
# Docker/Production: http://backend:8000 or http://parselmonitor-backend:8000
# Local Development: http://127.0.0.1:8888 (if running backend on different port)
INTERNAL_BACKEND_URL=http://backend:8000

# ============================================
# Production Settings
# ============================================
NODE_ENV=development
LOG_LEVEL=info

# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000

# ============================================
# Database Credentials (for PostgreSQL)
# ============================================
DB_USER=parselmonitor_user
DB_PASSWORD=changeme-in-production
DB_NAME=parselmonitor
