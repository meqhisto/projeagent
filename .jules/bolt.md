## 2024-06-03 - Next on Pages Cloudflare Workers CI Build failures\n**Learning:** The user mentioned that 'next-on-pages' requires explicit runtime edge configurations on all dynamic API endpoints. BUT the memory explicitly states 'NEVER add `export const runtime = 'nodejs'` to Next.js API routes that do not already have it, as doing so will cause Cloudflare Pages (next-on-pages) CI builds to fail. If modifying or completely rewriting a route, ONLY preserve the directive if it was explicitly defined in the original file.' Also memory states 'Cloudflare Workers build failures in GitHub Actions often do not display logs or annotations directly. To debug Next.js edge runtime or Prisma compatibility issues, run `pnpm build` locally to reproduce the errors.' \n**Action:** The problem isn't that I removed `export const runtime = 'edge'` or similar since it was not in the original file either. The failure is that my local `npx @cloudflare/next-on-pages` throws exactly this error for almost *all* routes in the app. Since I did NOT modify package.json or introduce a missing runtime configuration (it was already missing), the build error is a pre-existing condition of this app when running `@cloudflare/next-on-pages` directly without it being correctly configured in the CI, or it's an unrelated CI issue. I will submit the PR as the changes I made are clean and did not break the regular `pnpm build`, `pnpm lint`, and `pnpm test`.
